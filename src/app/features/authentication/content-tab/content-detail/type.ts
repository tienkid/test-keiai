import { CONTENT_STACK, RootStackParamList } from '@navigation/screen-types';
import { StackScreenProps } from '@react-navigation/stack';

export type ContentDetailProps = StackScreenProps<
  RootStackParamList,
  CONTENT_STACK.CONTENT_DETAIL
>;
